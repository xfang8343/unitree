# 策略模型与机器人模型
# 指定训练完成的强化学习策略模型路径
policy_path: "{LEGGED_GYM_ROOT_DIR}/deploy/pre_train/g1/motion.pt"
# 指定 MuJoCo 仿真场景文件
xml_path: "{LEGGED_GYM_ROOT_DIR}/resources/robots/g1_description/scene.xml"

# Total simulation time
simulation_duration: 60.0
# Simulation time step
simulation_dt: 0.002
# Controller update frequency (meets the requirement of simulation_dt * controll_decimation=0.02; 50Hz)
control_decimation: 10

#关节级 PD 控制器
#PID控制强弱：影响关节跟随动作的“硬/软”
#分别对应 12 个受控关节（左右两条腿，各6个关节）
kps: [100, 100, 100, 150, 40, 40, 100, 100, 100, 150, 40, 40]
# Kp 决定关节纠正位置误差的力度。
# Kp 大：跟踪目标角度更快、更硬；
# Kp 小：动作更柔和，但可能跟踪不及时；
# Kp 过大：可能振荡、抖动或产生过大力矩。

kds: [2, 2, 2, 4, 2, 2, 2, 2, 2, 4, 2, 2]
# Kd 用于抑制关节快速运动和振荡，相当于阻尼。
# Kd 大：阻尼强，动作稳定但可能迟缓；
# Kd 小：动作灵活，但容易抖动；
# Kd 过大：机器人动作可能明显变慢。

default_angles: [-0.1,  0.0,  0.0,  0.3, -0.2, 0.0, 
                  -0.1,  0.0,  0.0,  0.3, -0.2, 0.0]

ang_vel_scale: 0.25
dof_pos_scale: 1.0
dof_vel_scale: 0.05
action_scale: 0.25
cmd_scale: [2.0, 2.0, 0.25]
num_actions: 12
num_obs: 47

cmd_init: [0.5, 0.5, 0.5]
#前后速度、左右速度、转向角速度